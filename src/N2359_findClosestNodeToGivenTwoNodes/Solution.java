package N2359_findClosestNodeToGivenTwoNodes;

public class Solution {
    public int closestMeetingNode(int[] edges, int node1, int node2) {
        int[]edgedistances = new int[edges.length];
        for (int i = 0; i < edges.length; i++) {
            if(edges[i]==-1){
                continue;
            }
        }

        return -1;
    }
}
